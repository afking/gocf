package main

import (
	"github.com/afking/crazyflie/lib"
	_ "log"
)

func main() {
	/*
		// Setup driver
		ctx, dev, err := driver.Drive(true) // Currently debugging
		if err != nil {
			log.Fatal("CrazyRadio driver error: ", err)
		}
		defer driver.Close(ctx, dev)

		//dev.Open
	*/
}
